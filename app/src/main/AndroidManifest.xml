<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rtmillerprojects.sangitlive">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Green">
        <activity android:name=".ui.MainActivity"
            android:permission="android.permission.INTERNET">
            <uses-permission android:name="android.permission.INTERNET" />
            <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.ActivityArtistSearch"
            android:permission="android.permission.INTERNET" />
        <activity android:name=".ui.SetlistDetailActivity"
            android:permission="android.permission.INTERNET" />
        <activity android:name=".ui.EventDetailsActivity"
            android:permission="android.permission.INTERNET" />
        <activity android:name=".ui.ActivityArtistDetail"
            android:permission="android.permission.INTERNET" />
    </application>

</manifest>